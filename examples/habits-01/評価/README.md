# 評価 —— 外部評価（novel-council-layer）の実行メモ（巻一）

## 位置づけ

巻一の設計（`構想/design.md`・`構想/話割り.md`・`構想/各話/` の場面表14ファイル）に対する外部評価である（2026-09-19）。**執筆に入る前**の設計を対象とする。

- **評価するのは設計であって、散文ではない。** 散文はまだ一行も無い。ゆえに `content_type` は `plot`、招集は十名（`prose-style`・`narrative-technique`・`reader-experience` は招集外）。
- 五単位（巻一〜巻四＋系列）のひとつである。**単位の切り方・招集する席・匿名化・評議会に渡す文脈・SKILL.md からの逸脱・反映の作法は、系列の [`../../habits/評価/README.md`](../../habits/評価/README.md) に一本化して書いてある。ここには巻一に固有のことだけを書く。**

## 入力（準備済み・実測）

| ファイル | 内容 | 実測バイト |
|---|---|---|
| `input-concept.md` | `design.md` ＋ `話割り.md` ＋ `各話/*.md`（14ファイル）を**話順に逐語で連結**しただけ。**本文は一行も直していない・整形していない** | 122,428 |
| `input-anonymized.md` | **評価者に渡すのはこちら** | 122,428 |

**出典の数は 16。**（`design.md` 1 ＋ `話割り.md` 1 ＋ 各話 14）

## 匿名化について（実測）

`utils/anonymize.py` に `--title "ハビッツ！！！"` と `--title "ハビッツ"` を渡して実行。**赤字0件**であり、`input-concept.md` と `input-anonymized.md` は**バイト単位で同一**である。**作品名は設計文書に一度も現れない**——ゆえにこの入力では、第一の盲は入力そのものが満たしている。

## 実行方法

**novel-council-layer 内の新セッション**で [`../../habits/評価/起動カード.md`](../../habits/評価/起動カード.md) の枠内を貼る。巻一は**1番目**に走る。

> **本文は評価者に Read させる**（議長の文脈に通さない）。`SKILL.md` の「インラインで渡す」という字面からの意図的な逸脱である。理由は系列の README を参照。

## 期待される出力

`report.json` ／ `report.md`（`python utils/render_report.py report.json -o report.md --lang ja`）／ `individual_reports/`（評価者十名分の素の出力）。

## 本文評価（巻単位）——入力の写し（2026-09-26）

**⚠ これは上の設計評価とは別の回である。** 上が測るのは**設計（`plot`・十名）**、こちらが測るのは**本文（`text`・十三名・`full`）**である。**単位は巻**——**十四話を一つに連結して、一つの本文として読ませる**（**同型の先例＝第二巻の [`../../habits-02/評価/02_重なった名/`](../../habits-02/評価/02_重なった名/)、2026-09-21**）。

✅ **実行済み——2026-09-26。** 走らせたのは **`novel-council-layer` 内の新セッション**であり、[`../../habits/評価/起動カード_本文_第一巻と第四巻.md`](../../habits/評価/起動カード_本文_第一巻と第四巻.md) の枠内を貼った。**⚠ 同じカードで、第四巻も同じ日に走らせた**（[`../../habits-04/評価/README.md`](../../habits-04/評価/README.md)）。

| 項目 | 実測 |
|---|---|
| 招集 | **十三体**（`text` の `full`）。**`non_consulted_evaluators` も `excluded_evaluators` も空である** |
| 検証 | **十三体とも `valid: true`**（[`01_残った名/individual_reports/`](01_残った名/individual_reports/)）。**⚠ 再生成が一度、発動した**——**`narrative-technique` の出力が「トップレベルの対が早期に閉じ、以後が extra data として続く」形で落ちた**（[`01_残った名/report.json`](01_残った名/report.json) の caveats 第七条）。**規約どおり、手で直さず、同評価者を再生成した**。**⚠ 再生成の前後でスコアは動いていない**（66） |
| 集計 | **現在価値 67.17／潜在値 72.33**。分類は **`innovation`**（**⚠ 個別判定は discovery_target 10・current_success 3 に割れている**——**`innovation` は集計規則の出力であって、評価者の合意ではない**） |
| 出力 | [`01_残った名/report.json`](01_残った名/report.json)（209,738バイト）・[`report.md`](01_残った名/report.md)（92行）・`individual_reports/` 十三本 |
| 設計評価との比較 | **していない。** 上の設計評価は `plot`・十名・五単位であり、こちらは `text`・十三体・巻単位である（**数値の大小で比べない**——第二巻の回と同じ扱い） |

**⚠ 本文の写しは、評価の後も動かしていない**（**差が出たら申し送りに逐語で書く**、の規則どおりである）。

| 巻 | 写し | 実測 | 出どころ |
|---|---|---|---|
| 一『残った名』十四話 | [`01_残った名/input.md`](01_残った名/input.md) | **191,841バイト／13,228行（`wc -l`）／本文 6,565行・59,321字** | `../草稿/draft_01-01_…` 〜 `draft_01-14_…` を**番号順に単純連結**しただけ（**見出し行は残す。区切りは足していない**） |

**⚠ 同じ写しについて、二つの数え方がある**——**起動カードの「72,909字」は、改行と空白を含む生の文字数（`len(text)`）であり、「13,229行」は末尾の改行を一行として数える流儀（`str.split("\n")`）である。** **この表の 59,321字・6,565行は「本文だけ」の側であり、59,321 は題名行・空行・`## N` 見出しを除いた非空白文字である。** **⚠ どちらの数え方でも、写しは一つである。**

**見出しは14本**——1・728・1559・2440・3459・4402・5449・6450・7305・8332・9257・10252・11317・12370 行目（`# 第N話「…」`。**split の流儀**）。**非空である**（〔実測〕各行が `# 第N話「…」` の形をしている）。**各話は三節からなり、節の見出しは `## 1` 〜 `## 3` で42本である。**

**本文の字数は 59,321字であり、設計の予定 59,900 に対して −579 である**（**⚠ 予定字数は目安である**——2026-09-21、著者裁定・全巻。**ゆえに欠陥ではない**）。**十四話の長さは揃っている**——**最短 3,893字（第14話）／最長 4,574字（第7話）**で、**幅は 681字である**。

**⚠ 内面の欄は、十四話すべてに在る**（2026-09-21、著者裁定・反映済み。**値の内訳は A 20／B 12／C 10、なし 0**）。

### 匿名化について（実測）

`utils/anonymize.py` に `--title "ハビッツ！！！"` と `--title "ハビッツ"` を渡して実行。**赤字0件**であり、**前後はバイト単位で同一**（191,841 = 191,841）。**作品名は本文に一度も現れない**——ゆえにこの入力では、第一の盲は入力そのものが満たしている。

**⚠ 人物名は伏せない。** 人物名も渡すと **46件**が赤字になる（**すべて姓。碓氷 2・暮林 1・佐藤 19・片山 3・木村 9・秋山 6・石川 6。フルネームは 0件**）が、**第二巻・第三巻の巻単位の回と同じ判断で、渡さない**——**〔匿名〕を入れると、評価者が読む散文そのものが壊れる。**

**⚠ 評価の後で本文が動いても、写しは動かさない。** 差は申し送りに逐語で書く。

## 進捗

[`../../habits/評価/進捗.md`](../../habits/評価/進捗.md) ——五単位の進み具合と、著者待ちの一覧。**⚠ この巻の本文評価の記録は §14 に在る。**
