# 評価 —— 外部評価（novel-council-layer）の実行メモ（巻三）

## 位置づけ

巻三の設計（`構想/design.md`・`構想/話割り.md`・`構想/各話/` の場面表16ファイル）に対する外部評価である（2026-09-19）。**執筆に入る前**の設計を対象とする。

- **評価するのは設計であって、散文ではない。** 散文はまだ一行も無い。ゆえに `content_type` は `plot`、招集は十名（`prose-style`・`narrative-technique`・`reader-experience` は招集外）。
- 五単位（巻一〜巻四＋系列）のひとつである。**単位の切り方・招集する席・匿名化・評議会に渡す文脈・SKILL.md からの逸脱・反映の作法は、系列の [`../../habits/評価/README.md`](../../habits/評価/README.md) に一本化して書いてある。ここには巻三に固有のことだけを書く。**

## 入力（準備済み・実測）

| ファイル | 内容 | 実測バイト |
|---|---|---|
| `input-concept.md` | `design.md` ＋ `話割り.md` ＋ `各話/*.md`（16ファイル）を**話順に逐語で連結**しただけ。**本文は一行も直していない・整形していない** | 126,996 |
| `input-anonymized.md` | **評価者に渡すのはこちら** | 126,996 |

**出典の数は 18。**（`design.md` 1 ＋ `話割り.md` 1 ＋ 各話 16）

## 匿名化について（実測）

`utils/anonymize.py` に `--title "ハビッツ！！！"` と `--title "ハビッツ"` を渡して実行。**赤字0件**であり、`input-concept.md` と `input-anonymized.md` は**バイト単位で同一**である。**作品名は設計文書に一度も現れない**——ゆえにこの入力では、第一の盲は入力そのものが満たしている。

## 実行方法

**novel-council-layer 内の新セッション**で [`../../habits/評価/起動カード.md`](../../habits/評価/起動カード.md) の枠内を貼る。巻三は**3番目**に走る。

> **本文は評価者に Read させる**（議長の文脈に通さない）。`SKILL.md` の「インラインで渡す」という字面からの意図的な逸脱である。理由は系列の README を参照。

## 期待される出力

`report.json` ／ `report.md`（`python utils/render_report.py report.json -o report.md --lang ja`）／ `individual_reports/`（評価者十名分の素の出力）。

## 本文評価（巻単位）——入力の写し（2026-09-26）

**⚠ これは上の設計評価とは別の回である。** 上が測るのは**設計（`plot`・十名）**、こちらが測るのは**本文（`text`・十三名・`full`）**である。**単位は巻**——**十六話を一つに連結して、一つの本文として読ませる**（**同型の先例＝第二巻の [`../../habits-02/評価/02_重なった名/`](../../habits-02/評価/02_重なった名/)、2026-09-21**）。

✅ **実行済み——2026-09-26。** 走らせたのは **`novel-council-layer` 内の新セッション**であり、[`起動カード_本文.md`](起動カード_本文.md) の枠内を貼った。

| 項目 | 実測 |
|---|---|
| 招集 | **十三体**（`text` の `full`）。**`non_consulted_evaluators` も `excluded_evaluators` も空である** |
| 検証 | **十三体とも `valid: true`**（[`03_書き直した名/individual_reports/`](03_書き直した名/individual_reports/)）。**再生成は一度も発動していない** |
| 集計 | **現在価値 63.08／潜在値 76.33**。分類は **`innovation`**（**⚠ 個別判定は discovery_target 9・current_success 3・low_signal 1 に割れている**） |
| 出力 | [`03_書き直した名/report.json`](03_書き直した名/report.json)（242,647バイト）・[`report.md`](03_書き直した名/report.md)（101行）・`individual_reports/` 十三本 |
| 設計評価との比較 | **していない。** 上の設計評価は `plot`・十名・五単位であり、こちらは `text`・十三体・巻単位である（**数値の大小で比べない**——第二巻の回と同じ扱い） |

**⚠ 本文の写しは、評価の後も動かしていない**（**差が出たら申し送りに逐語で書く**、の規則どおりである）。

| 巻 | 写し | 実測 | 出どころ |
|---|---|---|---|
| 三『書き直した名』十六話 | [`03_書き直した名/input.md`](03_書き直した名/input.md) | **199,408バイト／73,722字／10,633行** | `../草稿/draft_03-01_…` 〜 `draft_03-16_…` を**番号順に単純連結**しただけ（**見出し行は残す。区切りは足していない**） |

**見出しは16本**——1・604・1287・1978・2677・3304・3983・4720・5401・6060・6759・7376・7999・8646・9335・10004 行目。**非空である。**

**作り方は、第二巻の巻単位の回から逆算して一致を確かめた**（同じ作り方で作り直して比べ、差は既知の評価後の改稿三箇所だけであった）。

### 匿名化について（実測）

`utils/anonymize.py` に `--title "ハビッツ！！！"` と `--title "ハビッツ"` を渡して実行。**赤字0件**であり、**前後はバイト単位で同一**（199,408 = 199,408）。**作品名は本文に一度も現れない**——ゆえにこの入力では、第一の盲は入力そのものが満たしている。

**⚠ 人物名は伏せない。** 人物名（侘田・篠原・長田・山田・田村・斎藤）も渡すと **15件**が赤字になる（侘田 10・篠原 1・長田 1・山田 1・田村 1・斎藤 1）が、**第二巻の巻単位の回と同じ判断で、渡さない**——**〔匿名〕を入れると、評価者が読む散文そのものが壊れる。**

**⚠ 評価の後で本文が動いても、写しは動かさない。** 差は申し送りに逐語で書く。

## 進捗

[`../../habits/評価/進捗.md`](../../habits/評価/進捗.md) ——五単位の進み具合と、著者待ちの一覧。
